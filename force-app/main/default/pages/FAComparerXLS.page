<apex:page controller="FAComparer" contentType="application/vnd.ms-excel" cache="true">
    <apex:pageBlock >
    <apex:pageBlockTable value="{!FieldList}" var="c">
      <apex:column > 
          <apex:facet name="header">{!objLabel} Field Name</apex:facet>
          <apex:outputText value="{!c.label}"/>
      </apex:column>
      <apex:column rendered="{!If(count>=1,true,false)}"  >
          <apex:facet name="header" >Record 1</apex:facet>
          <apex:outputText value="{!c.record1}"/>
      </apex:column>
      <apex:column rendered="{!If(count>=2,true,false)}" >
          <apex:facet name="header" >Record 2</apex:facet>
          <apex:outputText value="{!c.record2}"/>
      </apex:column>
      <apex:column rendered="{!If(count>=3,true,false)}" >
          <apex:facet name="header" >Record 3</apex:facet>
          <apex:outputText value="{!c.record3}"/>
      </apex:column>
      <apex:column rendered="{!If(count>=4,true,false)}" >
          <apex:facet name="header" >Record 4</apex:facet>
          <apex:outputText value="{!c.record4}"/>
      </apex:column>
      <apex:column rendered="{!If(count>=5,true,false)}" >
          <apex:facet name="header" >Record 5</apex:facet>
          <apex:outputText value="{!c.record5}"/>
      </apex:column>
      <apex:column rendered="{!If(count>=6,true,false)}" >
          <apex:facet name="header" >Record 6</apex:facet>
          <apex:outputText value="{!c.record6}"/>
      </apex:column>
      <apex:column rendered="{!If(count>=7,true,false)}" >
          <apex:facet name="header" >Record 7</apex:facet>
          <apex:outputText value="{!c.record7}"/>
      </apex:column>
      <apex:column rendered="{!If(count>=8,true,false)}" >
          <apex:facet name="header" >Record 8</apex:facet>
          <apex:outputText value="{!c.record8}"/>
      </apex:column>
      <apex:column rendered="{!If(count>=9,true,false)}" >
          <apex:facet name="header" >Record 9</apex:facet>
          <apex:outputText value="{!c.record9}"/>
      </apex:column>
      <apex:column rendered="{!If(count>=10,true,false)}" >
          <apex:facet name="header" >Record 10</apex:facet>
          <apex:outputText value="{!c.record10}"/>
      </apex:column>
      <apex:column rendered="{!If(count>=11,true,false)}" >
          <apex:facet name="header" >Record 11</apex:facet>
          <apex:outputText value="{!c.record11}"/>
      </apex:column>
      <apex:column rendered="{!If(count>=12,true,false)}" >
          <apex:facet name="header" >Record 12</apex:facet>
          <apex:outputText value="{!c.record12}"/>
      </apex:column>
      <apex:column rendered="{!If(count>=13,true,false)}" >
          <apex:facet name="header" >Record 13</apex:facet>
          <apex:outputText value="{!c.record13}"/>
      </apex:column>
      <apex:column rendered="{!If(count>=14,true,false)}" >
          <apex:facet name="header" >Record 14</apex:facet>
          <apex:outputText value="{!c.record14}"/>
      </apex:column>
      <apex:column rendered="{!If(count>=15,true,false)}" >
          <apex:facet name="header" >Record 15</apex:facet>
          <apex:outputText value="{!c.record15}"/>
      </apex:column>
      <apex:column rendered="{!If(count>=16,true,false)}" >
          <apex:facet name="header" >Record 16</apex:facet>
          <apex:outputText value="{!c.record16}"/>
      </apex:column>
      <apex:column rendered="{!If(count>=17,true,false)}" >
          <apex:facet name="header" >Record 17</apex:facet>
          <apex:outputText value="{!c.record17}"/>
      </apex:column>
      <apex:column rendered="{!If(count>=18,true,false)}" >
          <apex:facet name="header" >Record 18</apex:facet>
          <apex:outputText value="{!c.record18}"/>
      </apex:column>
      <apex:column rendered="{!If(count>=19,true,false)}" >
          <apex:facet name="header" >Record 19</apex:facet>
          <apex:outputText value="{!c.record19}"/>
      </apex:column>
      <apex:column rendered="{!If(count>=20,true,false)}" >
          <apex:facet name="header" >Record 20</apex:facet>
          <apex:outputText value="{!c.record20}"/>
      </apex:column>
  </apex:pageBlockTable>
  </apex:pageBlock>
<br/>
<br/>
Confidential Information - Do Not Distribute<br/>
Generated By: {!$User.FirstName} {!$User.LastName} {!NOW()}<br/>
GE Oil &amp; Gas
</apex:page>